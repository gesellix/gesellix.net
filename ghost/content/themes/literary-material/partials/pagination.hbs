<nav class="pagination" role="pagination">
  {{#if prev}}
    <a class="newer-posts" href="{{page_url prev}}"><i class="fa fa-chevron-left fa-fw"></i></a>
  {{/if}}
  <span class="page-number">{{page}} / {{pages}}</span>
  {{#if next}}
    <a class="older-posts" href="{{page_url next}}"><i class="fa fa-chevron-right fa-fw"></i></a>
  {{/if}}
</nav>
